<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>322 Todo</title>
		<link rel="stylesheet" href="/css/style.css" />
	</head>
	<body id="index-body">
		<nav>
			<div class="nav-container">
				<div class="brand">322 Todo</div>
				<li class="nav-item">Home</li>
				<li class="nav-item">Todo</li>
				<li class="nav-item">Shopping</li>
				<a href="/user/logout"><button id="logout-btn">Logout</button></a>
			</div>
		</nav>
		<% if (user.hasGroup == true) { %>
		<div class="group-dashboard">
			<h1>your group's dashboard</h1>
		</div>
		<% } else { %>
		<h1>Set up your household</h1>
		<div class="container" id="household-setup-container">
			<div class="form-control">
				<button id="create-household-btn">Create household</button>
			</div>
			<form id="household-join-form" action="/household/join" method="POST">
				<label for="household-code">Or join an existing household</label>
				<div class="form-control" id="join-form-control">
					<input
						type="text"
						id="household-code"
						name="household-code"
						placeholder="Enter access code"
					/>
					<button id="join-btn">Join</button>
				</div>
			</form>
		</div>
		<% } %>
	</body>
</html>
